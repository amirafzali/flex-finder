/**
 * File: ProfileFlow.tsx
 * Date: January 1st, 2021
 * Details: CONTROLLER/FLOW FUNCTIONS: Controller to manage Profile Views
*/

import { useState } from "react";
import Button from "@mui/material/Button";
import {Container, Row, Col} from "react-bootstrap";
import { useNavigate, useLocation } from "react-router-dom";

enum ProfilePages {
    VIEW
}

// Main controller FLOW transitioning between different views
export function ProfileFlow(){
    // TODO - Complete
    // TODO - Make additional components for the specific views (similar to search and scheduler components!)
}